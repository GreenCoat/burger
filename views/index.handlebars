<h1>Eat-Da-Burger!</h1>
<h2>Delicious Burgers Waiting To Be Devoured!</h2>
<ul>
  {{#each burgers}}
    {{#unless devoured}}
      <li>
        {{burger_name}}
        <button class="devour" data-id="{{id}}">DEVOUR</button>
      </li>
    {{/unless}}
  {{/each}}
</ul>
<h2>Notes On The Delicious Burgers Of Times Past!</h2>
<ul>
  {{#each burgers}}
    {{#if devoured}}
      <li>
        {{burger_name}}
      </li>
    {{/if}}
  {{/each}}
</ul>
<h2>Order a Burger</h2>
<form class="create-form">
  <div class="form-group">
    <label for="burger">Burger Order:</label>
    <input type="text" id="burger" name="burger">
  </div>
  <button type="submit">Order!</button>
</form>